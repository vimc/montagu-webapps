import * as React from "react";
import {PageHeader} from "../../shared/components/PageWithHeader/PageHeader";

export const ContribPageHeader: React.SFC<{}> = () => (
    <PageHeader siteTitle={"Modellers' contribution portal"} />
);